# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real keys.

# Required only when using the gpt4v model
OPENAI_API_KEY=sk-...

# Optional: set log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
